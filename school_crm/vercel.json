{
  "buildCommand": "cd deployment && pip install -r requirements.txt && python manage.py collectstatic --noinput --ignore=admin",
  "outputDirectory": "deployment/school_crm/staticfiles",
  "env": {
    "PYTHONUNBUFFERED": "1",
    "DJANGO_SETTINGS_MODULE": "deployment.school_crm.settings"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "deployment.school_crm.wsgi"
    }
  ]
}
