{
  "buildCommand": "cd school_crm && pip install -r ../requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput",
  "outputDirectory": "school_crm/staticfiles",
  "env": {
    "PYTHONUNBUFFERED": "1"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "school_crm/wsgi.py"
    }
  ]
}
