{
  "buildCommand": "pip install -r requirements.txt && cd school_crm && python manage.py migrate && python manage.py collectstatic --noinput",
  "outputDirectory": "school_crm/staticfiles",
  "env": {
    "PYTHONUNBUFFERED": "1"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "wsgi.py"
    }
  ]
}
